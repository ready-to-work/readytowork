<!DOCTYPE html>
<html lang="en">

<head>
	<!-- Meta tags -->
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <meta name="description" content="Ready to Work is an application made for COGS 120 at UCSD.">
    <meta name="author" content="Alina Chin, Timothy Bian, Zi Long Pan">
    <link rel="icon" type="image/ico" href="favicon.ico">

	<title>Ready to Work</title>

	<!-- Stylesheets -->
	{{> bootstrapCSS}}
	<link rel="stylesheet" href="/css/tasks.css">
</head>

<body>
	<!-- Navbar -->
	{{> navbar}}

	<div class="container">
		<div class="row">
			<div class="col">
				<p id="teamName">{{currTeam.teamName}} tasks</p> 
			</div>
		</div>

		<div class="row">
			<div class="col">
				<div class="card" id="addMenu">
					<div class="card-header font-bold" id="addButton">create new task</div>
					<div class="card-body" id="addTaskForm">
						<form role="form" method="get" action="/teamlist/team/{{currTeam.teamID}}/tasks/add">
						    <div class="form-group">
						      <input type="text" class="form-control"placeholder="task title" name="title">
						    </div>
						    <div class="form-group">
						      <input type="text" class="form-control" placeholder="priority" name="priority">
						    </div>
						    <div class="form-group">
						      <input type="text" class="form-control" placeholder="due date" name="due_date">
						    </div>
						    <div class="form-group">
						      <input type="text" class="form-control" placeholder="description" name="description">
						    </div>
						    <input type="submit" id="submitBtn" class="btn-block" value="create"></input>
						</form>
					</div>
				</div>
			</div>
		</div>

		<div class="row">
			<div class="col">
				{{#each currTeam.tasks}}
				<div class="card task-card">
					<p class="card-header">{{title}}</p>
					<div class="card-body">
						<p><span class="font-bold">priority: </span>{{priority}}</p>
						<p><span class="font-bold">due date: </span>{{due_date}}</p>
						<p><span class="font-bold">description: </span>{{description}}</p>
					</div>
				</div>
				{{/each}}
			</div>
		</div>
	</div>
	
	<!-- Scripts -->
	{{> bootstrapJS}}
	<script src="/js/main.js"></script>
	<script src="/js/tasks.js"></script>

</body>

</html>