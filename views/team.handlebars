<!DOCTYPE html>
<html lang="en">

<head>
	<!-- Meta tags -->
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <meta name="description" content="Ready to Work is an application made for COGS 120 at UCSD.">
    <meta name="author" content="Alina Chin, Timothy Bian, Zi Long Pan">
    <link rel="icon" type="image/ico" href="favicon.ico">

	<title>Ready to Work</title>

	<!-- Stylesheets -->
	{{> bootstrapCSS}}
	<link rel="stylesheet" href="/css/team.css">
</head>

<body>
	<!-- Navbar -->
	{{> navbar}}

	<!-- Page -->
	<div class="container">
		<div class="row">
			<div class="col">
				<h1>{{currTeam.teamName}}</h1> 
			</div>
		</div>

		<div class="row">
			<div class="col">
				<div class="card card-box" id="calendar-box">
					<a href="/teamlist/team/{{currTeam.teamID}}/calendar">
						<h2 class="card-header font-bold">Team Calendar</h2>
					</a>
					<div class="card-block">
						{{#each currTeam.events}}
						<div class="item">
							<div class="item-title">{{title}}</div>
							<div class="item-info font-italic">{{time}}</div>
							<div class="item-info font-italic">{{location}}</div>
						</div>
						{{/each}}
					</div>
				</div>
			</div>
		</div>

		<div class="row">
			<div class="col">
				<div class="card card-box" id="task-box">
					<a href="/teamlist/team/{{currTeam.teamID}}/tasks">
						<h2 class="card-header font-bold">Team Tasks</h2>
					</a>
					<div class="card-block">
						{{#each currTeam.tasks}}
						<div class="item">
							<div class="item-title">{{title}}</div>
							<div class="item-info font-italic">priority: {{priority}}, due {{due_date}}</div>
						</div>
						{{/each}}
					</div>
				</div>
			</div>
		</div>
	</div>
	
	<!-- Scripts -->
	{{> bootstrapJS}}
	<script src="/js/main.js"></script>
</body>

</html>